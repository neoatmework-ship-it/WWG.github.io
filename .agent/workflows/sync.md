---
description: Automatically stages, commits, and pushes all gallery updates.
---

// turbo-all

1. Stage all changes with `git add .`
2. Create a commit with `git commit -m "Auto-sync: Reality Hacks and Gallery updates"`
3. Push to the main branch with `git push`
